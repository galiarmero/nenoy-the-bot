{
    "framework": null,
    "installCommand": "npm install",
    "buildCommand": "npm run fetch-puzzles && mkdir -p public && touch public/.keep",
    "outputDirectory": "public",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api/serverless.js"
        }
    ]
}
